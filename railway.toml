[build]
builder = "nixpacks"
buildCommand = "cd backend && dotnet publish -c Release -o /app/publish"

[deploy]
startCommand = "cd /app/publish && dotnet PublicCarRental.dll --urls http://0.0.0.0:$PORT"